Runs a provider and persists the result in transactions.


Batch provider like directory will call this service with the details for a file provider
directory heeft geen output dus kan niet gepost processed worden

post process neemt 1 uri als input en output potentieel meerdere uris?
of omgekeerd -> meerdere uris en 1 (bv. zip input?)
many to one is exceptional? als ge écht wilt zippen kan dit applicatief, meer files is de normale usecase eerder dan minder
-> indien ge batch size kunt aansturen (of alle files in 1 service call), is het genoeg om ze tegelijk te verwerken in geval er interdependencies zijn

elke uri van de post process moet opgeslagen zijn (input van spec is uris) dus gaat ook in de tabel komen
elke uri kan dan aan een aparte taak hangen (afhankelijk van batch size) dus moet ook opgeslagen kunnen worden

als een provider afgerond is -> checken hoeveel er open staan, wat voor batch size ge wilt en de batches opstarten in tasks en updaten

als alleen one to many, dan kan dit per entry gezien worden (we moeten niet naar de batch kijken)

alle entries hebben een boolean of ze "finaal" zijn of niet
-> bv. Dir entry is nooit finaal
-> file entry is finaal _als_ er geen post processing stap is
-> anders is er een finale post processing stap en zijn de entries daarvan finaal